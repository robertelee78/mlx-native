/target

# Git worktrees — long-lived parallel branches (moved from
# /opt/mlx-native-adr013 sibling-path convention to nested on 2026-04-24).
.worktrees/
.cfa-worktrees/

# Local dev tooling
.mcp.json
.claude/
.claude-flow/
CLAUDE.md

# BMAD session research artifacts (transient — distilled into ADRs/docs)
_bmad-output/

# Python fixture caches
tests/fixtures/__pycache__/
