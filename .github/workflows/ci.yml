name: ci

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rust-metal-train-forward-d256:
    runs-on: macos-15
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - name: Toolchain
        run: rustup toolchain install stable --profile minimal
      - name: Forward D256 shader isolation
        run: >-
          cargo +stable test --locked --test test_flash_attn_train
          flash_attn_train_tests::test_d256_library_compiles_with_l_out --
          --exact --test-threads=1

  rust-metal:
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
      - name: Toolchain
        run: rustup toolchain install stable --profile minimal
      - name: Test
        run: cargo +stable test --locked -- --test-threads=1
      - name: Package
        run: cargo +stable package --locked
